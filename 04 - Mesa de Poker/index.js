const inputValores = [1, 2, 3, 4, 5, 6, 7, 8, 9]
const inputMin = 2
const inputMax = 6

let listaAtualizada = [];

for (let i = 0; i < valores.length; i++) {
    if (valores[i] >= min && valores[i] <= max) {
        listaAtualizada.push(valores[i]);
    }
} console.log(listaAtualizada);
